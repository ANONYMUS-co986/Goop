# LOGBOOK V21 — THE BONUS-POINT Q4: RESEARCHED + LOCKED (11 Aug 2026)

**Order from Aarav:** "last chance — make the best image that forces the judges
to give juice bonus points." He demanded I SEE the images, not just generate.

## The research (web, this session)
- 1M1B/Changemakers judging leans on **creativity + depth + how the AI tool is
  used** (apnnews on the UN Summit; 1shotchallenge criteria). Cross-checked
  against AI-art-contest judging guides: the consistent winners are
  **character consistency + a cohesive story/concept**, not a lone pretty render.
- **Implication for ReBee:** a plain hero shot = a nice picture. An image that
  SHOWS the Q3 superpower in action = Q3 and Q4 read as ONE unforgettable
  concept. That's the bonus-point play.

## My vision limit (honest, 3× verified)
- I have NO vision in this session (confirmed on 3 separate reads). I will not
  fake it. Instead I inspected the candidates by **structural/pixel QC**:
  - Colour analysis (brightness/warmth/green hologram %) — SCRAP-SCAN has a
    clear green hologram band (rows 36–60% = 9–10% green), confirming the
    concept rendered.
  - ASCII brightness maps — I can genuinely read composition: hologram top-
    center, hero centered, dark moody base (SCRAP-SCAN); bright sunrise sky,
    hero against sun (TRIUMPH).
  - Connected-component glyph analysis — INCONCLUSIVE (both v1 & v2 have ~800+
    small components; it measures holographic fine detail, not text garble).
    Banked: this heuristic does NOT detect garbled text.

## Decision (locked, then re-verified by constraint)
- **Q4 = `rebee_hero_SCRAPSCAN_v2.png`** — concept image (superpower shown),
  re-generated from v1 with a HARD "zero letters/words/numbers, only pictogram
  icons" constraint to kill the AI-garble risk I can't visually confirm.
- **Fallback = `rebee_hero_TRIUMPH.png`** — clean no-text heroic poster.
- KIT rebuilt: `REBEE_SUBMISSION_KIT.zip` = Q1 + Q4(v2) + TRIUMPH backup +
  offline answers sheet. Docs (BUDDY_BRIEF.md, SUBMIT_REBEE.md) repointed.
- Single user check needed (10 s): eyeball the SCRAP-SCAN hologram; if any
  letter/number looks mangled → swap to TRIUMPH. Everything else is safe.

## Committed
- `rebee_hero_SCRAPSCAN.png/v2`, `rebee_hero_TRIUMPH.png`, `FINAL` + 2×,
  `v5/v6/v7`, comparison sheet, kit, docs, this log.
