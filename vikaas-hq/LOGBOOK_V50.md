# LOGBOOK V50 — THE VIDEO REVIEWER + YT VERDICT + TASK-2 PHYSICAL PLAN (12 Aug 2026)

**Order:** view the Flash 3 brief video "at any cost" — try every method for a YT
downloader; build a video reviewer/self-checker; plan Task 2 physical work.

## 1. YOUTUBE DOWNLOAD — every method tried, verdict: BLOCKED from sandbox
- pip yt-dlp (PyPI open) ✓ installed
- Actual download attempt → YouTube API TLS-EOF (000)
- Egress probes: youtube.com / yt-dl.org / invidious.f5.si / yewtu.be /
  inv.nadeko.net = ALL 000
- **Verdict: the sandbox cannot reach YouTube by any channel. USER ACTION:**
  download the Flash 3 brief video on your phone/laptop with any open-source
  downloader (yt-dlp, NewPipe, cobalt.tools) and COMMIT it to repo `main` →
  I fetch it from GitHub (open channel) → the reviewer processes it.

## 2. THE VIDEO REVIEWER + SELF-CHECKER (built, tested) — engine/video_review.sh
- One command on any local mp4: duration/size → LUFS + silence audit →
  N-frame extraction → per-frame stats (blank/dark/white flags) →
  near-duplicate md5 detection → contact sheet → ASCII storyboard (watchable) →
  verdict file.
- TESTED on VIKAAS_04: 40.8s · −16.1 LUFS exact · 0 dropouts · 0 dups ·
  1 DARK flag (fade-in, legitimate) · storyboard read scenes (title card →
  dark text scene → receipt card).
- This is the "eyes for video": once the Flash 3 video lands in the repo, I
  extract frames + storyboard it + transcribe the brief + self-check it.

## 3. TASK 2 (M2 "Act & Measure") — the physical checklist for Aarav
(Full detail in PLAN.md — this is the 90-min minimum + the full version.)
1. WEIGH-DAY: empty your drawer(s), weigh on kitchen scale, photograph
   (scale + items), log in the tracker. (Already did 1.4 kg — redo on camera
   for the reel-of-truth.)
2. SURVEY 12 households: print/send the baseline survey (5 questions), record
   answers. (We did 10 — push to 12+.)
3. RECYCLER CALLS: call 3–5 HSPCB list numbers; log: who answers, minimum kg,
   doorstep pickup? (The "bulk-only" finding = our argument.)
4. SOCIETY COLLECTION DRIVE: one Sunday — WhatsApp the society message (written
   in workspace), collect + weigh everything, photograph, handover via a
   COMPLIANT route (producer take-back / Croma / Namo / Karo Sambhav / MCG —
   NOT bulk B2B), log kg + ₹.
5. REEL-OF-TRUTH: film the real drawer + scale reading on your phone (30–60s).
6. PROOF PACK: all photos, tracker sheet, receipts → upload to repo main
   (or send) → I build the M2 submission + evidence album.
7. 24h-insights: screenshot after every post (already live — M1/M2/V2 posted).

## 4. FLASH 3 — pending the video
Once the video is in the repo: I review it (storyboard + transcribe) → I plan
the submission → you execute the physical part (if any) → we submit before the
deadline.

## 5. Portfolio
Continuing next turn: Phase 11 (the scale sequence). The Gate is 8/16
NIRMAN-sections complete; app-explainer phases 25–28 planned.
