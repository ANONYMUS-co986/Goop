# LOGBOOK V25 — LOADER v2 "THE BOOT": SCROLL-CINEMATIC + 3D (11 Aug 2026)

**Order:** loader felt unthought → re-think first, plan the timeline, max anims,
scroll-driven, code-built 3D assets. User also locked: over-the-top scroll cinema
on EVERY page · LOTS of pages · FULL ambient audio (I generate it).

## The re-think (timeline first, then code)
460vh pinned scroll track, scrub 0.6, 10 beats documented in MANUAL §9:
HUD → terminal → camera push → lid open + e-waste float → scramble word →
stats + stamps → ReBee fly → big line → ENTER.

## Code-built 3D assets (Three.js r160 UMD vendored)
- The DRAWER: metal box + canvas-texture VIKAAS front panel + green wireframe
  edges + hinged lid (pivot group) + base
- E-WASTE: phone (box + emissive screen) · 2 cables (TubeGeometry along
  CatmullRom curves) · battery · charger (+prong cylinder) · PCB (+edge lines)
- 420-particle dust cloud (additive blending, brand palette)
- Lights: ambient + directional + acid point + green rim
- Mouse parallax on camera · items bob via sin loop

## Audio (all generated by me)
boot.wav (C5-E5-G5, −13.3 dB) on first gesture · enter.wav (thump+whoosh+stab,
−7.0 dB) on click · runtime WebAudio whooshes at beat transitions (bandpass
noise sweeps) — autoplay-safe (unlock on wheel/touch/pointer).

## Self-review receipts (headless walk of all 9 beats)
- STATES: word=VIKAAS ✓ dev=1 ✓ stats=4/4 ✓ stamps=4/4 ✓ enterShow ✓ webgl ✓
  **0 console/page errors** ✓
- Pixel rig: acid% 148→658 across beats, bright% jumps at ReBee fly (15.35) and
  ENTER (7.49), no blanks
- **BUGS CAUGHT BY THE LOOP (all fixed):** (1) GSAP onUpdate receives NO args —
  `self` undefined → use `this`; (2) `tl.call(fn, pos)` needs params ARRAY —
  bare number → CreateListFromArrayLike crash; (3) ReBee img path wrong
  (../cwc_buddy → ../../cwc_buddy) → 404; (4) #stats overlapped #enter →
  clicks intercepted (real UX bug — a judge would've hit it too); (5) harness
  measured scroll extent before pin-spacer existed → wrong scroll targets.

## Files
portfolio/v2/loader.html + assets/js/loader.js (rewritten) · vendor/three.min.js
(0.160.0) · qa/ (9 beat screenshots) · MANUAL §9 timeline · engine/shoot_boot.js

## Next
Phase 2 THE GATE on user GO. ReBee submission still pending (deadline TOMORROW).
